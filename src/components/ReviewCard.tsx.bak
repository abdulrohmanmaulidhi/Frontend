import React from "react";
import starIcon from "../assets/icon/star.png";

interface ReviewCardProps {
  image: string;
  packageName: string;
  tourName: string;
  rating: number;
}

function Star() {
  return (
    <div className="shrink-0 size-[28px] md:size-[38px]">
      <img src={starIcon} alt="star" className="w-full h-full object-contain" />
    </div>
  );
}

export default function ReviewCard({ image, packageName, tourName, rating }: ReviewCardProps) {
  return (
    <div className="bg-white relative rounded-[20px] w-full">
      <div className="absolute border border-[#ffb4c4] inset-0 rounded-[20px] pointer-events-none shadow-[0px_12px_16px_-4px_rgba(10,13,18,0.08),0px_4px_6px_-2px_rgba(10,13,18,0.03)]" />

      <div className="flex flex-col md:flex-row items-center w-full">
        <div className="flex flex-col md:flex-row gap-4 md:gap-[27px] items-center px-4 md:px-[50px] py-6 md:py-0 w-full">
          
          {/* IMAGE */}
          <div className="h-[250px] md:h-[373px] w-full md:w-[630px] relative shrink-0">
            <div className="absolute inset-0 rounded-[10px] shadow-[0px_2px_4px_-2px_rgba(10,13,18,0.06),0px_4px_8px_-2px_rgba(10,13,18,0.1)]">
              <img
                alt={tourName}
                src={image}
                className="absolute inset-0 w-full h-full object-cover rounded-[10px]"
              />
            </div>
          </div>

          {/* INFO */}
          <div className="flex flex-col items-start w-full md:w-[461px]">

            <div className="w-full">
              <div className="flex items-center justify-center w-full p-[10px]">
                <p className="font-bold text-[#444444] text-[28px] md:text-[40px] text-center">
                  {packageName}
                </p>
              </div>
            </div>

            <div className="flex flex-col items-center w-full">

              <div className="w-full flex items-center justify-center p-[10px]">
                <p className="font-medium text-[#444444] text-[20px] md:text-[30px] text-center">
                  {tourName}
                </p>
              </div>

              <div className="flex items-center justify-center w-full">
                <div className="p-[8px] md:p-[10px]">
                  <Star />
                </div>

                <div className="p-[8px] md:p-[10px]">
                  <p className="font-medium text-[#444444] text-[20px] md:text-[30px]">
                    {rating.toFixed(1)}
                  </p>
                </div>
              </div>

            </div>

          </div>

        </div>
      </div>

    </div>
  );
}
